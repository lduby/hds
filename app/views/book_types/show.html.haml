%br
%h1= @book_type.name
%br
.page-header
  %strong All Books
- if @book_type.books.size == 0
  %p= "No books found for "+@book_type.name+"."
- else 
  %ul.thumbnails{:id => "grid"}
    - @book_type.books.each do |book|
      %li
        .thumbnail
          %a{:href => book_path(book)}
            = image_tag(book.cover.url(:tiny))