= form_tag "/reviews/select_shelf" do
  = hidden_field_tag 'book_id', @book.id
  = hidden_field_tag 'old_shelf_id', @book_shelf.id
  = select_tag "new_shelf_id", options_from_collection_for_select(current_user.profile.shelves, "id", "name")
  = submit_tag "Change shelf"
